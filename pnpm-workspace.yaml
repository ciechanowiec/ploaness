# Workspace linking is local; published packages use exact cross-package versions.
packages:
  - packages/*

linkWorkspacePackages: true

engineStrict: true

onlyBuiltDependencies: []

allowBuilds:
  esbuild: false

minimumReleaseAgeStrict: true

overrides:
  deepmerge-ts: '^8.0.2'
