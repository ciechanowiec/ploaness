{
  "$comment": "The framework-shaped half: tsconfig-core.json carries the strictness, this adds what only a Next application needs.",
  "extends": "./tsconfig-core.json",
  "compilerOptions": {
    "plugins": [
      {
        "name": "next"
      }
    ]
  }
}
