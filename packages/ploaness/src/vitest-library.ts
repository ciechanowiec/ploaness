// See eslint.js for why every entry point is re-exported through this package. The library variant runs
// in node with no React plugin, because a package with no browser has no DOM to simulate.
export { default } from '@ploaness/config/vitest-library'
