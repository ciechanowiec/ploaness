// See eslint.js for why every entry point is re-exported through this package. This is the variant a
// member with no application receives: the same caps and bans, without the blocks keyed to paths only a
// Payload application has.
export { default } from '@ploaness/config/eslint-library'
