#!/usr/bin/env node
// The `ploaness` command. The implementation lives in @ploaness/cli; this file exists so the executable
// is owned by the package a consumer actually declares.
import '@ploaness/cli/bin'
