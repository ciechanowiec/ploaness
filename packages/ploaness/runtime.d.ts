// A project's own source imports this, and that source is type-checked at full strictness, so the shape
// has to be declared rather than inferred from a JavaScript file.
export { safeHref } from '@ploaness/governance'
